# 📐 Arquitetura Visual do MDProject

## Layout Desktop com Split View ✨

```
┌─────────────────────────────────────────────────────────────────┐
│  ← Sidebar Toggle  │  Documento   │ 📂 3  │ 🔀 Split  👁️ Theme │
├─────────────────┬──────────────────────────────────────────────┤
│                 │                                              │
│    SIDEBAR      │              EDITOR + PREVIEW               │
│                 │  ┌─────────────────┬──────────────────────┐ │
│  • Doc 1        │  │  EDITOR (Cole)  │  PREVIEW (Render)   │ │
│  • Doc 2   ───→ │  │                 │                      │ │
│  • Doc 3        │  │  # Título       │  # Título            │ │
│                 │  │  **Negrito**    │  **Negrito**         │ │
│  Buscar: _____  │  │                 │  • Lista             │ │
│                 │  │  - Item 1       │  - Item 1            │ │
│                 │  └─────────────────┼──────────────────────┘ │
│                 │  💾 | H1 H2 H3 | B I C | Code | Table ...   │
└─────────────────┴──────────────────────────────────────────────┘
```

## Layout Desktop sem Split View

```
┌──────────────────────────────────────────────────────────────┐
│  ← |  Documento  │ 📂 3  │ ✏️ Editor | 👁️ Preview | ← Voltar   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                    EDITOR FULL WIDTH                        │
│  💾 | H1 H2 H3 | B I C | Code | Table | List | Link |      │
│                                                              │
│  # Seu Titulo                                              │
│                                                              │
│  Comece a escrever seu conteudo em Markdown...             │
│                                                              │
│                                                              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## Layout Mobile

```
┌──────────────────────────────┐
│  ✏️  MDProject  🌙            │ ← Header
├──────────────────────────────┤
│  ← Voltar  │ Doc Title       │ ← Mobile Header
├──────────────────────────────┤
│                              │
│        EDITOR FULL WIDTH     │
│                              │
│  💾 | H1 H2 H3 ...          │
│                              │
│  # Titulo                    │
│                              │
│  Escreva seu conteúdo...    │
│                              │
│                              │
└──────────────────────────────┘

(Alterne com ✏️/👁️ para preview)
```

## Fluxo de Dados

```
┌─────────────────────────────────────────────────────────┐
│                    APP STATE                             │
│                                                          │
│  currentDocument ──→ Document selecionado               │
│  content ──→ Markdown raw                               │
│  showSplitView ──→ Ativa/desativa split                │
│  isEditorMode ──→ Editor (true) ou Preview (false)    │
│  theme ──→ Light ou Dark                                │
│                                                          │
└──────────────┬────────────────────────────────┬──────────┘
               │                                │
        ┌──────▼──────┐              ┌──────────▼──────┐
        │    Editor   │              │     Preview     │
        │             │              │                 │
        │ Textarea    │ onChange     │ Rendered HTML   │
        │ Toolbar     ├─────────────→│                 │
        │ Auto-save   │   content    │ Theme colors    │
        └─────────────┘              └─────────────────┘
                │                          ▲
                │  StorageService.save     │
                └─────────────────────────→│
                         └──────────────────┘
                         (LocalStorage)
```

## Estado de Tela

### Desktop (>1024px)
```
showSplitView = TRUE (padrão)
isEditorMode = TRUE

Layout:
┌─────────────────────────────┐
│  Sidebar  │ Editor | Preview │
└─────────────────────────────┘
```

### Tablet (768px - 1024px)
```
showSplitView = FALSE
isEditorMode = TOGGLE

Layout:
┌──────────────────┐
│ Editor ou Preview │  (Alternados)
└──────────────────┘
```

### Mobile (<768px)
```
showSplitView = FALSE (forçado)
isEditorMode = TOGGLE

Layout:
┌──────────────┐
│ E ou P (tela │  (100% width)
└──────────────┘
```

## Component Hierarchy

```
App
├── NewDocumentModal
├── Header (Mobile)
├── Flex Container
│   ├── Sidebar (Desktop)
│   └── Main Content
│       ├── Header (Desktop)
│       ├── Content Flex
│       │   ├── MarkdownEditor
│       │   │   ├── Toolbar
│       │   │   ├── Textarea
│       │   │   └── TableModal
│       │   ├── Divider (Split Only)
│       │   └── ContentEditor
│       │       └── MarkdownPreview
│       └── (Preview Modal Mobile)
└── DocumentGallery
```

## Theme System

```
Theme (Light/Dark)
    ↓
getTheme(theme)
    ↓
themeColors object
    ├── background
    ├── surface
    ├── text
    ├── textTertiary
    ├── primary
    ├── hover
    ├── border
    ├── shadow
    ├── warning
    └── success
    ↓
Applied to Components
```

## Data Flow: Saving

```
User digita
    ↓
onChange(newContent)
    ↓
setContent(newContent)
    ↓
useEffect detecta mudança
    ↓
setSaveStatus("💾 Salvando...")
    ↓
setTimeout 2000ms
    ↓
StorageService.saveDocument(updated)
    ↓
LocalStorage atualiza
    ↓
setSaveStatus("✅ Salvo")
    ↓
setTimeout 2000ms
    ↓
setSaveStatus("")
```

## Storage Structure

```
LocalStorage
├── mdproject_docs (JSON Array)
│   ├── { id, title, content, coverImage, createdAt, updatedAt }
│   ├── { id, title, content, coverImage, createdAt, updatedAt }
│   └── { ... }
└── mdproject_theme (String)
    └── "light" ou "dark"
```

---

**Diagrama atualizado: Nov 5, 2025**
